% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autonls_vnext.R
\name{AutoNLS}
\alias{AutoNLS}
\title{Fit AutoNLS vNext models with one workflow}
\usage{
AutoNLS(
  data,
  x,
  y,
  models = c("Hill", "Logistic", "Gompertz"),
  weights_col = NULL,
  loss = c("mse", "mae", "huber", "quantile"),
  n_starts = 25,
  seed = 42,
  optimizer = c("optim", "nlsLM"),
  quantile_level = 0.5,
  huber_delta = 1,
  lower_bounds = NULL,
  upper_bounds = NULL,
  maxit = 5000,
  reltol = 1e-08,
  scale_x = TRUE,
  scale_y = TRUE,
  model_status = c("stable", "experimental", "all"),
  validation_fraction = 0,
  validation_seed = 42,
  interval_method = c("none", "residual_bootstrap", "parametric_simulation"),
  interval_level = 0.95,
  interval_n = 200,
  interval_seed = 42,
  interval_models = c("best", "all"),
  interval_max_rows = 1000,
  start_strategy = c("family", "generic", "log_transformed", "family_transformed"),
  theme = "dark"
)
}
\arguments{
\item{data}{A data.frame or data.table.}

\item{x}{Predictor column name.}

\item{y}{Target column name.}

\item{models}{Character vector of model names, or "all".}

\item{weights_col}{Optional non-negative weights column.}

\item{loss}{One of "mse", "mae", "huber", or "quantile".}

\item{n_starts}{Number of deterministic starts per model.}

\item{seed}{Random seed used for start generation.}

\item{optimizer}{One of "optim" or "nlsLM". \code{nlsLM} is used only when available.}

\item{quantile_level}{Quantile used when loss = "quantile".}

\item{huber_delta}{Delta used when loss = "huber".}

\item{lower_bounds}{Optional named vector/list overriding lower bounds.}

\item{upper_bounds}{Optional named vector/list overriding upper bounds.}

\item{maxit}{Maximum optimizer iterations.}

\item{reltol}{Relative tolerance for \code{optim}.}

\item{scale_x}{Logical. Fit models on scaled x.}

\item{scale_y}{Logical. Fit models on scaled y and back-transform predictions.}

\item{model_status}{One of "stable", "experimental", or "all".}

\item{validation_fraction}{Optional holdout fraction between 0 and 0.5.}

\item{validation_seed}{Seed used for validation split.}

\item{interval_method}{One of "none", "residual_bootstrap", or "parametric_simulation".}

\item{interval_level}{Confidence level for prediction intervals.}

\item{interval_n}{Number of bootstrap/simulation draws when intervals are enabled.}

\item{interval_seed}{Seed used for interval simulation.}

\item{interval_models}{One of "best" or "all".}

\item{interval_max_rows}{Maximum x-grid rows used for interval curves.}

\item{start_strategy}{Internal start strategy. Defaults to family-aware starts.}

\item{theme}{Plot theme metadata retained on the fit object.}
}
\value{
An AutoNLSFit R6 object.
}
\description{
Fit AutoNLS vNext models with one workflow
}
