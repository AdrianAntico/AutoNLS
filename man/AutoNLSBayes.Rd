% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesian_response.R
\name{AutoNLSBayes}
\alias{AutoNLSBayes}
\title{Fit a Bayesian nonlinear response curve with optional linear controls}
\usage{
AutoNLSBayes(
  data,
  x,
  y,
  family = "Logistic",
  controls = NULL,
  model_domain = NULL,
  priors = NULL,
  chains = 4L,
  iter = 2000L,
  burnin = 1000L,
  thin = 2L,
  seed = 42L,
  proposal_scale = 1,
  control_standardize = TRUE
)
}
\arguments{
\item{data}{A data.frame or data.table containing observed rows only.}

\item{x, y}{Focal nonlinear predictor and outcome column names.}

\item{family}{One qualified equation name from \code{\link[=list_nls_models]{list_nls_models()}}.}

\item{controls}{Optional character vector of linear control columns.}

\item{model_domain}{Optional numeric length-two natural/domain support for \code{x}.}

\item{priors}{Optional named prior overrides. Names are equation parameters,
\verb{control:<column>}, and \code{sigma}. Unspecified priors are weak, generated,
recorded, and returned on the fit.}

\item{chains}{Number of independent random-walk Metropolis chains.}

\item{iter, burnin, thin}{MCMC iterations, discarded warmup, and thinning.}

\item{seed}{Random seed.}

\item{proposal_scale}{Positive proposal multiplier.}

\item{control_standardize}{Center and scale controls internally for sampling.}
}
\value{
An \code{AutoNLSBayesFit} R6 object retaining draws and diagnostics.
}
\description{
Fits \verb{y = f(x; theta) + Z beta + error} using one existing AutoNLS equation
family and zero or more jointly estimated linear controls. Bayesian fitting is
explicit and does not alter \code{\link[=AutoNLS]{AutoNLS()}}.
}
\examples{
set.seed(4)
d <- data.frame(sentiment = runif(80, -.4, 0), impressions = runif(80, 80, 140))
d$visits <- 20 + 35 / (1 + exp(-10 * (d$sentiment + .15))) +
  .08 * (d$impressions - 110) + rnorm(80, 0, 2)
fit <- AutoNLSBayes(d, "sentiment", "visits", family = "Logistic",
  controls = "impressions", model_domain = c(-1, 1),
  chains = 2, iter = 400, burnin = 200)
fit$predict(data.frame(sentiment = c(-.2, .3), impressions = 110))
}
